<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>de.gematik.zeta.zeta-guard</groupId>
        <artifactId>keycloak-zeta</artifactId>
        <version>1.0.1</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <modelVersion>4.0.0</modelVersion>
    <artifactId>hsm-token-signing</artifactId>
    <name>𝝵-Guard :: HSM Token Signing Key Provider</name>
    <description>
        Keycloak KeyProvider SPI plugin that exposes an HSM-backed EC key for JWT token signing.
        Delegates all signing to the HSM Proxy via gRPC — no private key material ever leaves the HSM.
    </description>

    <dependencies>
        <dependency>
            <groupId>org.keycloak</groupId>
            <artifactId>keycloak-server-spi</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.keycloak</groupId>
            <artifactId>keycloak-server-spi-private</artifactId>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>org.keycloak</groupId>
            <artifactId>keycloak-core</artifactId>
            <scope>provided</scope>
        </dependency>

        <!--
            Thin JAR dependency — provides HsmProxyProvider, HsmKeyStoreSpi, HsmEcPrivateKey, etc.
            The full gRPC/protobuf runtime is already bundled in zeta-hsm-proxy-provider.jar (fat JAR)
            which Keycloak loads from providers/ before this JAR — no shading needed here.
        -->
        <dependency>
            <groupId>de.gematik.zeta.zeta-guard</groupId>
            <artifactId>java-hsm-proxy-provider</artifactId>
            <version>${java-hsm-proxy-provider.version}</version>
        </dependency>

        <!-- Test -->
        <dependency>
            <groupId>io.kotest</groupId>
            <artifactId>kotest-runner-junit5-jvm</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.kotest</groupId>
            <artifactId>kotest-assertions-core-jvm</artifactId>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>io.mockk</groupId>
            <artifactId>mockk-jvm</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>
